@model Shop.Models.Spaceships.ImageViewModel

@{
    var photoPaths = "~/multibleFileUpload/" + (Model.FilePath ?? "noimage.jpg");
}

<img class ="img-fluid-cover" src="@photoPaths" asp-append-version="true" />
